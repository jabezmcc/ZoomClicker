After changing things locally, do 
git add --all

then do
git commit -m "some note"

then do 
git push origin main
(you will need username and access token)

AFter changing on the repository (and committing), do
git pull origin main
(you will need username and access token)



